# Win7 Start Menu - Standalone
هذا مشروع APK مستقل، وليس نسخة معدلة داخل OpenNavBar.
Build من GitHub Actions ينتج app-debug.apk.
بعد تثبيته: افتح التطبيق، امنح Overlay، ثم شغّل Start Menu.
الربط مع الضغط المطول في OpenNavBar يتم من إعداد OpenNavBar إذا كان يدعم Launch App.
